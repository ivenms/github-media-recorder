// Utility for uploading files to server
// - Chunked upload, progress, retries, error handling

export async function uploadFile(file: Blob, onProgress?: (p: number) => void): Promise<void> {
  // Upload logic here
} 