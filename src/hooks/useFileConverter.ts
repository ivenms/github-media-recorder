import { useState, useCallback } from 'react';

export function useFileConverter() {
  // Handles FFmpeg.js initialization, conversion, progress, and errors
  // Returns convert function, progress, and error state
  return {
    // convert, progress, error
  };
} 